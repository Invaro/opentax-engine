/** Compatibility shim — the composers live in @invaro/opentax-compose (per-state modules). */
export { composeStateReturn, stateReturnShape } from "@invaro/opentax-compose";
export type { StateReturnInput, StateTaxEvaluator } from "@invaro/opentax-compose";
